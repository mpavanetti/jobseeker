apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: jenkins
spec:
  template:
    spec:
      containers:
        - name: jenkins
          env:
            - name: CASC_JENKINS_CONFIG
              value: /var/jenkins_files/casc.yaml,/var/jenkins_files/casc-kubernetes-standalone.yaml
